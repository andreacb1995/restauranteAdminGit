import { bootstrapApplication } from '@angular/platform-browser';
import { appConfig } from './app/app.config';
import { AdministradorComponent } from './app/app.component';

bootstrapApplication(AdministradorComponent, {
  providers: []
});